class linked_node:
	def __init__(self, value=None):
		self.value = value
		self.next = None

class linked_list:
	def __init__(self):
		self.head = None

	def add_node(data):
		new_node = new linked_node(data)
		this.linked_node = linked_node()

newList = linked_list()
